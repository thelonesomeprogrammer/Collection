layout {
    pane split_direction="horizontal" {
        pane {
            command "cargo"
            args "watch" "-x run"
            cwd "backend/"
        }
        pane { 
            command "trunk"
            args "watch"
            cwd "ui/"
        }
        pane { 
            command "docker"
            args "run" "--rm" "-p 80:8000" "-v surreal:/mydata" "surrealdb/surrealdb:latest" "start" "--log trace" "--user root" "--pass root" "file:mydatabase.db"
            cwd "/home/marrinus/"
        }
    }
}